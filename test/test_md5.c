#include <assert.h>
#include "md5.h"
#include <stdio.h>

int main()
{
    // // Test addition
    // assert(add(2, 3) == 5);
    // assert(add(-1, 1) == 0);

    // // Test subtraction
    // assert(subtract(5, 3) == 2);
    // assert(subtract(0, 3) == -3);

    printf("All tests passed!\n");
    return 0;
}